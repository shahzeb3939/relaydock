relay.example.com {
  encode zstd gzip
  # The web container already proxies /api and /ws to the server. Caddy
  # preserves WebSocket upgrades automatically. Change this address if Caddy
  # itself runs inside the RelayDock Compose network.
  reverse_proxy 127.0.0.1:8080
}
